apiVersion: v1
kind: ConfigMap
metadata:
  name: back-configuration
  namespace: prioriza
  labels:
    app: prioriza
data:
  RABBIT_HOST: "mq-rabbit-service"
  RABBIT_PORT: "80"
  REDIS_HOST: "db-redis-service"
  REDIS_PORT: "80"
